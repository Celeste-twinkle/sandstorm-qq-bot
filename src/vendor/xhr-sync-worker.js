// Placeholder for jsdom's optional sync XHR worker path.
// The bot does not use synchronous XMLHttpRequest; the bundled runtime only
// needs this file to exist because jsdom calls require.resolve at startup.
